# ============================================
# Fumorive Backend Requirements
# Python 3.10+
# Last Updated: 13 Januari 2026
# ============================================

# Web Framework
fastapi==0.109.0
uvicorn[standard]==0.27.0
python-multipart==0.0.6

# Database & ORM
sqlalchemy==2.0.25
alembic==1.13.1
psycopg2-binary==2.9.9  # PostgreSQL adapter
asyncpg==0.30.0  # Async PostgreSQL

# Authentication & Security
python-jose[cryptography]==3.3.0  # JWT tokens
passlib[bcrypt]==1.7.4  # Password hashing
python-dotenv==1.0.0  # Environment variables
firebase-admin==6.5.0  # Firebase Admin SDK for OAuth

# Data Validation
pydantic==2.5.3
pydantic-settings==2.1.0
email-validator==2.1.0  # Required by pydantic EmailStr

# WebSocket & Real-time
websockets==12.0

# Caching
redis==5.0.1
hiredis==2.3.2  # Redis performance boost

# Serialization
msgpack==1.0.7  # Binary serialization (faster than JSON)

# CORS (sudah built-in di FastAPI, tidak perlu package terpisah)

# Testing
pytest==7.4.4
pytest-asyncio==0.23.3
httpx==0.26.0  # Async HTTP client for testing

# Development Tools
black==24.1.1  # Code formatter
flake8==7.0.0  # Linter
mypy==1.8.0  # Type checker

# Monitoring & Logging
prometheus-fastapi-instrumentator==6.1.0
prometheus-client==0.24.1
python-json-logger==2.0.7
psutil==5.9.8         # system metrics (CPU, memory, disk) for /health endpoint

# Rate Limiting
slowapi==0.1.9

# Utilities
uuid==1.30

