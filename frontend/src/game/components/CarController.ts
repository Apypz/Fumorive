/**
 * CarController Re-export
 * =======================
 * This file re-exports from the new modular location for backward compatibility.
 * 
 * New location: ./car/CarController.ts
 * 
 * @deprecated Import from './car' or '../game' instead
 */

export { 
  CarController, 
  DEFAULT_CAMERA_CONFIG,
  type CarControllerConfig,
  type CameraMode,
  type ControlMode,
  type CameraPositionConfig,
} from './car'
